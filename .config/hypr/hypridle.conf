general {
    before_sleep_cmd = hyprlock                        # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on         # ensure display turns on
}

listener {
    timeout = 600                                      # 3.5min - lock screen
    on-timeout = hyprlock
}

listener {
    timeout = 720                                      # 5.5min - screen off
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

listener {
    timeout = 800                                      # 10min - suspend
    on-timeout = systemctl suspend
}
